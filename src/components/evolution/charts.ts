// `chart.js/auto` registers every controller/element/scale, so the Line and
// Bar components always have what they need at runtime.
import 'chart.js/auto';

export const GREEN = '#36d36b';
export const GREEN_DIM = 'rgba(54,211,107,0.18)';
export const GRID = 'rgba(120,160,120,0.12)';
export const TICK = '#6f8a73';
