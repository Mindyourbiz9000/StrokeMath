# Supabase — copy to .env.local and fill in.
# The app runs fully offline (localStorage) when these are absent;
# when present, sessions sync to Supabase Postgres.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: POST endpoint to receive client crash reports (JSON).
VITE_ERROR_WEBHOOK=
